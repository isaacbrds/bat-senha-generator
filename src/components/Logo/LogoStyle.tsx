import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  text: {
    color: 'gold',
    alignItems: 'center',
    textAlign: 'center',
    marginTop: 20,
  },
  image: {
    resizeMode: 'contain',
    height: 180,
  },
});